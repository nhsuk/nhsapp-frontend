{% extends "./sidebar.njk" %}

{%- from "./partials/side-navigation.njk" import appSideNavigation %}

{% block beforeContent %}
  {% if title === "Patterns" %}
    {{ breadcrumb({
      href: "/",
      text: "Home"
    }) }}
  {% else %}
    {{ breadcrumb({
      items: [
        {
          href: "/",
          text: "Home"
        }
      ],
      href: "/patterns",
      text: "Patterns"
    }) }}
  {% endif %}
{% endblock %}

{% set sidebar %}
  {{ appSideNavigation({
    label: 'Patterns',
    currentPath: page.url,
    sections: [
      {
        heading: {
          text: 'Screens'
        },
        items: collections.screen | sort(attribute="data.order") | withNestedItems(collections.integratingServices | sort(attribute="data.order"), "/patterns/integrating-services/")
      },
            {
        heading: {
          text: 'Structure'
        },
        items: collections.structure | sort(attribute="data.order") | withNestedItems(collections.integratingServices | sort(attribute="data.order"), "/patterns/integrating-services/")
      },
      {
        heading: {
          text: 'Help users to'
        },
        items: collections.helpUsers | sort(attribute="data.order")
      }
    ]
  }) }}
{% endset %}

{% block content %}
  {% if childPage %}
    <span class="nhsuk-caption-xl">Integrating services into the NHS App</span>
  {% elif tags and "screen" in tags %}
    <span class="nhsuk-caption-xl">Screens</span>
  {% elif tags and "structure" in tags %}
    <span class="nhsuk-caption-xl">Structure</span>  
  {% elif tags and "helpUsers" in tags %}
    <span class="nhsuk-caption-xl">Help users to</span>
  {% endif %}
  <h1 class="nhsuk-heading-xl nhsuk-u-margin-bottom-5">
    {{ title }}
  </h1>
  {% if pageNotice %}
    {% include "layouts/partials/" + pageNotice + ".njk" %}
  {% endif %}
  <p>{{ description }}</p>
  {{ content | safe }}
  {% if tags and ("screen" in tags or "helpUsers" in tags or "structure" in tags) %}
    {% include "layouts/partials/feedback-section.njk" %}
  {% endif %}
{% endblock %}
