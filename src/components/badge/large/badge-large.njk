{%- if params.count -%}
<span class="nhsapp-badge {{ params.classes if params.classes }}">
    {% if params.count > 9 %}
        9+
    {% else %}
        {{ params.count }}
    {% endif %}
    <span class="nhsuk-u-visually-hidden">{{ params.label }}</span>
</span>
{%- endif -%}