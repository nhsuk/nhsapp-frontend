{% import "../icon/icons.njk" as iconHtml %}

{% macro nhsappIcon(iconName, classes) %}
  {% set svg = iconHtml[iconName]() %}
  {% if classes %}
    {{ svg | replace('class="', 'class="' + classes + ' ') | safe }}
  {% else %}
    {{ svg | safe }}
  {% endif %}
{% endmacro %}
